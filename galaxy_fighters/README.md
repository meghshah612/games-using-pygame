# Galaxy Fighters

Run the game:

- Using the module: `python -m galaxy_fighters`
- Directly: `python main.py`

Files:
- `main.py` - game logic and entrypoint
- `assets/` - image and sound assets
